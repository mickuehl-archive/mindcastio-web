
<search>
	<%= simple_form_for :search, :url => "/search/#{@page}" do |f| %>
		<%= f.input :query, label: false, placeholder: 'Search for podcasts, episodes, authors or anything else ...' %>
	<% end %>
</search>
